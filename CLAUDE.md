# mostek-kultura — pravidla projektu

Komunikuj česky. Stručnost a přímost, minimum omáčky. Identifikátory v kódu a docstringy anglicky, prózu (README, komentáře k rozhodnutím, commity) česky.

## Co to je

Denní agregátor kulturních akcí v okolí obce Mostek. GitHub Actions (cron 04:30 UTC) spustí `python -m mostek_kultura build`, výsledek (`site/`) jde na GitHub Pages: https://ivolarys.github.io/mostkultura/. Home Assistant čte `summary.json` (REST senzory) a stránku zobrazuje v iframe (`?embed=1`). Plán: `~/.claude/plans/cht-l-bych-ud-lat-aplikaci-validated-quasar.md`.

## Delegování práce na subagenty (modely)

- **GPT-6 Astra (`gpt-6-astra`)**: hlavní model pro návrhy, analýzy, přípravu zadání, review, testy, kontroly a orchestraci (preference uživatele pro celý projekt, 2026-09-12).
- **Levnější modely**: samotné kódování delegovat na GPT-5.6 Luna, u složitější implementace na GPT-5.6 Terra nebo Sol. Výsledek kontroluje Astra. Použít všude, kde prostředí umožňuje volbu modelu; změnu modelu běžícího tahu nepředstírat.

## Jak pracovat

- Prostředí: `.venv` vytvořený přes `uv venv --python 3.13 .venv`, závislosti `uv pip install -e .[dev]`.
- Testy: `.venv/bin/pytest -q` (offline, fixtures v `tests/fixtures/<zdroj>/`). Lint: `.venv/bin/ruff check mostek_kultura`.
- Lokální build bez sítě a bez LLM: `python -m mostek_kultura build --offline --no-llm`.
- Nový zdroj existujícího typu = jen záznam v `config.yaml`. Nový typ = modul v `mostek_kultura/sources/` + registrace v `sources/__init__.py` + fixture (`build --record --source <name> --no-llm`) + test.
- Fixtures se nahrávají příkazem `build --record` (zapíše `tests/fixtures/<zdroj>/manifest.json` + soubory). Po změně URL v configu nahrát znovu.
- `cache/last_good/` zapisuje jen CI (env `CI`) nebo `build --persist`; lokální živé buildy ho nemění, aby nevznikaly konflikty s commity z CI. Když konflikt přesto vznikne, vezmi novější verzi souboru.
- LLM klasifikace: `OPENAI_API_KEY` (model `OPENAI_MODEL`, výchozí `gpt-5-mini`) nebo `ANTHROPIC_API_KEY` (`claude-haiku-4-5`); `LLM_PROVIDER` vynutí volbu. Bez klíče jen klíčová slova.
- `cache/` je commitovaná (klasifikace z LLM + poslední dobrý fetch per zdroj). CI ji commituje zpět s `[skip ci]`. Ruční oprava kategorie: editovat `cache/classifications.json`.
- Akce z Facebooku (nedají se stahovat) se doplňují ručně do `manual_events.yaml` (zdroj `rucne`, typ `manual`); formát viz komentář v souboru.
- Testy i offline build jsou na "dnešek" citlivé jen zdánlivě: `dates.now()/today()` respektují env `MOSTEK_NOW` (ISO datum/datetime), testy si ho v `conftest.py` sami nastaví na den nahrání fixtures. Ruční reprodukce toho dne: `MOSTEK_NOW=2026-09-11 python -m mostek_kultura build --offline --no-llm`. `FixtureHttp` navíc při přesném netrefení URL v manifestu zkusí normalizovaný dotaz (stejná cesta a množina klíčů, ignoruje se `rok=`/`mesic=`/`datum_od=`/`datum_do=`/`page=` a hodnoty vypadající jako datum/číslo) – proto zdroje stavějící URL z dnešního data (`public4u`, `vismo`) fungují offline i jiný den.

## Poznatky (s datem)

- 2026-09-11: GoOut API ignoruje geo parametry na `/schedules`; funguje `/venues?query=<město>` + `/schedules?venueIds[]=`. Max `limit=48`. `languages[]=cs` je povinné.
- 2026-09-11: Public4u má dva šablonové výpisy: mudk.cz (karty, jen datum) a trutnov.cz (tabulka s místem a časem). Pro mudk se dotahuje detail.
- 2026-09-11: mostek.cz (Galileo) vrací 503 na RSS, HTML jde s browser User-Agentem. `data-date-start` s časem 00:00:00 = bez času.
- 2026-09-11: mestovrchlabi.cz (Drupal 9): teaser má `<time datetime>` ISO a typ, detail adresu a konec; stránkování `?page=N` po 12.
- 2026-09-11: Galileo má dvě šablony výpisu: `.event-action__item` (mostek.cz) a starší `div.event.event-message` (bilatremesna.cz, dolnibrusnice.cz, kuks.cz); parser umí obě.
- 2026-09-11: hospital-kuks.cz (NPÚ) renderuje jen aktuální okno akcí, měsíční záložky jsou JS. domovsvatehojosefa.cz má akce na homepage (`article.b-article`), bez času.
- 2026-09-11: hkregion.cz má jinou šablonu než ostatní Public4u weby, zatím nepoužito.
- 2026-09-11: Vismo (WEBHOUSE) – mujicin.cz, munovapaka.cz: `kalendar-akci.asp?hledani=1&kdy=-1&datum_od=D.M.YYYY&datum_do=D.M.YYYY&pocet=100&submit=Vyhledat` (`id_org` není potřeba, dovodí se z domény; `pocet=100` obvykle obejde stránkování, `.strvpred a.aktivni` je záložní další strana). Výpis `#kalendarAkci .dok ul.ui > li`: `.n5-akce-datum` má datum/čas/místo pohromadě ("17.9.2026 18:00, Klenotnice muzea"), `.n5-akce-popis`, `.n5-akce-typ a`; čas i místo jsou rovnou ve výpisu, detail není potřeba. Nedatované "Dlouhodobé akce" se ukážou jen na nefiltrované výchozí stránce, dotaz na rozsah data je automaticky vynechá. mujicin.cz má tento kalendář fakticky mrtvý (poslední záznam z r. 2022) – zdroj `jicin` je zapojený správně, jen momentálně vrací 0 akcí.
- 2026-09-11: kultura-novapaka.cz (kulturní portál MKS a partnerů) je iso-8859-2, server-rendered, bez date-range dotazu i stránkování – vždy jen aktuální okno ~2–3 týdny (podobně jako hospital-kuks.cz). Položky `div.program <kategorie>` (druhá třída bývá se stopovací mezerou navíc), `.datum span` s minutami v `<sup>` ("11.&nbsp;září&nbsp;2026 od&nbsp;20<sup>00</sup>"), místo v `<p><strong>Kde</strong>: ...`.
- 2026-09-11: Přidání obce "Jičín" do `places` odkrylo prioritní chybu v `resolve_places`: dřív vyhrával `place_raw` nad `venue`, takže konkrétní venue jako "Valdštejnská lodžie" (GoOut vrací place_raw = město "Jičín") by spadlo pod obecné "Jičín" místo vlastního bucketu. Opraveno na `venue` napřed, `place_raw` jako fallback (test `test_lodzie_is_its_own_place`).
- 2026-09-12: uffo.cz/program běží na stavovém ASP.NET e-shopu ("Business Site") – přepínání měsíce, "Dnes/Zítra/O víkendu" i stránkování jde jen přes podepsané AJAX postbacky, `?month=`/`?rok=` v URL se ignoruje (redirect zpět na holé `/program/`), takže prostý GET vrátí vždy jen aktuální kalendářní měsíc (`div.productHolder[showdate]` = "so 12", bez měsíce/roku). Detaily akcí (`/<slug>-p<id>/`) mají jen Product JSON-LD (cena/obrázek/popis pro e-shop), Event JSON-LD nikde není. Skutečně čistý zdroj dat je ICS feed z odkazu "Přidat do kalendáře" (`/uffoCompleteCalendar.ashx/?c=apple` → 302 na `/data/user-content/calendar/completeCalendar.ics`): nefoldovaný plochý seznam `VEVENT` (UID/DTSTART/DTEND s `TZID=Europe/Prague`/SUMMARY/LOCATION, DESCRIPTION vždy prázdný), sahá měsíce dopředu (~120 akcí do příštího roku) a UID jsou stabilní napříč requesty. LOCATION je "UFFO", "Kino Vesmír" nebo prázdné. Protože ICS nemá URL/kategorii/obrázek, dopočítává se to spárováním (normalizovaný název + den v měsíci + HH:MM, jen v rámci aktuálního měsíce) na karty `div.ProductView` z výpisu (`h2 a`, `.dcTimeWrap`, `.icon.productFlag.cs_typeIco[title]` = nativní kategorie, u kina chybí → "Film", `img[data-src]`, `p.description`); akce mimo viditelný měsíc zůstanou bez url/kategorie/obrázku (url spadne na `/program/`). Křížová deduplikace s městským kalendářem Trutnova (zdroj `trutnov`) funguje – v ofline buildu se sloučila "Dny evropského dědictví" (shoduje se datum i podobnost názvu).
- 2026-09-12: `uffo.fetch` páruje ICS položky na kartičky z výpisu jen když `today().month` sedí s měsícem akce (viz výše) – to je nezávislé na `FixtureHttp`/`MOSTEK_NOW` fallbacku pro URL (výpis `uffo` má stálou URL bez data v query, takže se vždy trefí přesně). Simulace budoucnosti (`MOSTEK_NOW=2026-12-01`) proto smysluplně rozbije `test_uffo` (nic nespáruje, `native_category` je `None`) – to je vlastnost samotného zdroje, ne chyba fixture/HTTP fallbacku; při běžném běhu testů (frozen na den nahrání fixtures) i při reálném živém buildu (výpis vždy ukazuje aktuální měsíc) je to v pořádku.
- 2026-09-12: biografnaspici.cz (Hořice, ne Jaroměř – v patičce/adrese je jen Hořice) běží na "mojekino"/Cinemaware-Ticketware šabloně: `/program`/`/klient-101/kino-30` je jen prázdná schránka, výpis dotahuje JS z `GET /ajax/ajax_program_kina_rozsireny.php` (klient/kino/smid z `data-*` atributů shellu, `sTab=&sTabID=&jazyk=` prázdné = výchozí "hraje se" záložka, `pocet_na_strane=100` obejde stránkování po 10). Datum bez roku ("12. 9.&nbsp;dnes"), `span.btn.price[data-id]` je stabilní napříč requesty a slouží jako native_id.
- 2026-09-12: galerie.horice.org (Vismo6/WEBHOUSE, novější produkt než klasický `kalendar-akci.asp`) má "Wecal" kalendář na `/kalendar-udalosti`, taky jen prázdná šablona doplněná z `GET /api/services/app/Object/GetEventsByDay?From=...&To=...&ObjectId=...&ElementTypeOfObjectTypeBlockId=...` (id z `<meta data-id-object>` a `data-element-type-of-object-type-block-id`). Datum `From`/`To` je záměrně statický široký rozsah (ne odvozený od "dnes") – jinak by se URL měnila každý den a fixture matching (`FixtureHttp._find_fallback`) tenhle ISO formát nepozná. Kalendář v praxi obsahuje jen tři testovací "Nová událost v kalendáři" záznamy z dubna 2026 – reálně nepoužívaný.
- 2026-09-12: dum-kultury-koruna.cz je v Hořicích (potvrzeno "sál hořické radnice" v popisech akcí). `/program` je bespoke server-rendered CMS (žádný známý engine), páry `div.b-text` v `div.mt-i-c`: první datum/čas ikonami `i.fa-calendar`/`i.glyphicon-time` ("D.M." bez roku, "HH:MMh"), druhý název a popis; žádné stránkování, vždy jen aktuální okno.
- 2026-09-12: epo1.cz (Webflow) má `/doprovodny-program` s kalendářem, který NENÍ Webflow CMS Collection (žádné `w-dyn-*` třídy) ale ručně/CMS generované pole `var EVENTS=[...]` přímo v `<script id="epo1-calendar-engine">` – validní JSON po odstranění `var EVENTS=`/`;`. Pole `date`/`time` (může být rozsah s pomlčkou en-dash)/`place`/`type`/`desc`/`slug`/`url` (goout.net nebo relativní vlastní stránka s `img`).
- 2026-09-12: belohradskasypka.cz (Webnode) `/program/`: každá akce je dvojice sourozeneckých `div.b.b-text` v `div.mt-i-c.cf.mt-border.line-color` (datum+místo+odkaz / název+popis); `parse_cz` zvládne i tvar "od čtvrtka 9. 7. 2026" (trvalá nabídka typu kavárny/výstavy). Odkaz na detail se recykluje pro opakující se akci (týdenní kvíz), native_id proto kombinuje odkaz a čas začátku.
- 2026-09-12: mestys-pecka.cz běží na Galileo CMS (starší šablona `div.event.event-message`), kalendář na `/volny-cas/kalendar-akci-1/` – nový zdroj typu `galileo`, žádný nový parser.
- 2026-09-12: umeleckakoloniejosefov.cz (WordPress, plugin "Google Calendar Events"/simcal, synced z Google Kalendáře) na `/kalendar-akci/` server-renderuje jen aktuální měsíc (přepínání měsíce jde přes `wp-admin/admin-ajax.php` POSTem s `action=simcal_default_calendar_draw_grid`, což sdílený `Http` klient (jen GET) neumí). Vícedenní akce se v mřížce opakují pro každý den rozsahu – dedup přes `eid` z odkazu na Google Kalendář. Plné ISO datum s offsetem je v atributu `content` u `span.simcal-event-start`/`-end`; `location` bývá jen název akce znovu (Google Kalendář nemá zvlášť pole na místo).
- 2026-09-12: klasterhostinne.cz (bespoke CMS "ERmedia") nemá kalendář akcí, jen `Aktuality` na homepage (`#ArticleSection > div.item`, sdílené pro muzeum/knihovnu/galerii) – běžný výpis podle data publikace mixuje ohlédnutí za proběhlými akcemi s pozvánkami, ale web sám značí už proběhlé položky třídou `span.archiveItem` ("událost již proběhla"), takže se dá spolehlivě odfiltrovat, co je ještě dopředu.
- 2026-09-12: muzeum.horice.org (klasický Vismo5, `/vismo/o_utvar.asp`) nemá `kalendar-akci.asp` (404) a jeho `/ostatni-program` je jen archiv tiskových zpráv o proběhlých akcích (datováno podle publikace, ne podle konání) – žádný strojově čitelný budoucí kalendář, zdroj vynechán.
- 2026-09-12: Mapa („Seznam/Mapa“ na dashboardu) geokóduje přes Nominatim (`mostek_kultura/geocode.py`), cache `cache/geocode.json` (commitovaná jako `classifications.json`). Nominatim policy: max 1 req/s (`SLEEP_S=1.1`), vlastní `User-Agent`, `--geocode-max` (výchozí 40) limituje nové dotazy za běh – při větší dávce nových venue je potřeba build spustit víckrát (zbytek mezitím spadne na centroid obce). Bare dotaz `"<obec>, Česko"` je nespolehlivý (např. jinde v ČR existuje jiné "Mostek" u Ústí nad Orlicí) – proto se u "okres Trutnov" obcí přidává `okres Trutnov` do dotazu; Jičín/Hořice/Nová Paka/Lázně Bělohrad/Pecka jsou okres Jičín, tam se ověřuje shoda jména obce v `display_name` výsledku místo přidávání okresu. Bbox sanity check (50.2–50.9 / 15.2–16.3) navíc odchytí i zbylé nesmysly.
- 2026-09-12: mestovrchlabi.cz a klasterhostinne.cz (hosting Wedos, 62.109.15x.x) jsou z GitHub runnerů občas nedostupné i přes pinned IP (timeout). Uživatel rozhodl nechat to být (záložní data z last_good), neřešit timerem ani self-hosted runnerem.
